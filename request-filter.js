(function(){'use strict';
/* Request Saya filter: status comes from the status badge, and re-applies briefly after tab clicks so UI re-renders cannot reset it. */
let mode='all';
function statusOf(el){if(!el)return'waiting';const badge=el.querySelector('.myreq-status,.myreq-feature-status');const t=(badge?badge.textContent:el.textContent||'').toLowerCase().replace(/\s+/g,' ').trim();if(t.includes('ditolak')||t.includes('tolak'))return'reject';if(t.includes('selesai')||t.includes('played'))return'done';if(t.includes('diproses')||t.includes('proses'))return'process';return'waiting';}
function match(el){const s=statusOf(el);return mode==='all'||(mode==='waiting'&&s==='waiting')||(mode==='done'&&s==='done');}
function apply(){const section=document.getElementById('myRequestsSection');if(!section)return;const feature=section.querySelector('.myreq-feature');const cards=section.querySelectorAll('.myreq-card');if(feature)feature.style.display=match(feature)?'':'none';for(let i=0;i<cards.length;i++)cards[i].style.display=match(cards[i])?'':'none';let empty=section.querySelector('.myreq-filter-empty');let visible=0;if(feature&&feature.style.display!=='none')visible++;for(let i=0;i<cards.length;i++)if(cards[i].style.display!=='none')visible++;if(!visible){if(!empty){empty=document.createElement('div');empty.className='myreq-filter-empty';empty.style='padding:14px;text-align:center;color:#64748b;font-size:10px;border:1px dashed #16413e;border-radius:10px;margin-top:8px;';const list=section.querySelector('.myreq-list');if(list)list.appendChild(empty);}empty.textContent=mode==='waiting'?'Belum ada request yang masih dalam antrean.':mode==='done'?'Belum ada request yang selesai.':'Belum ada request.';}else if(empty)empty.remove();}
function applySoon(){apply();requestAnimationFrame(apply);setTimeout(apply,60);setTimeout(apply,180);setTimeout(apply,400);setTimeout(apply,800);}
function setMode(next){mode=next;applySoon();}
function handleClick(e){const btn=e.target&&e.target.closest?e.target.closest('button'):null;if(!btn||btn.closest('#myRequestsSection'))return;const label=(btn.textContent||'').replace(/\s+/g,' ').trim().toLowerCase();if(label==='semua')setMode('all');else if(label==='antrean')setMode('waiting');else if(label==='selesai')setMode('done');}
function init(){document.addEventListener('click',handleClick,false);setTimeout(apply,300);}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
