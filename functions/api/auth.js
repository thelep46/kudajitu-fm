const GAS = 'https://script.google.com/macros/s/AKfycbyUB8drjL1dSJedYjKIKjVc5gzIE3Pe-QS0FF8o1_zU4NkAweGLFquhHLfy1Nt_eITA-Q/exec';
const ALLOWED_ACTIONS = new Set(['login','session','logout','adminlogin','adminsession','adminlogout','forgotpassword','changepassword']);
async function handleAuth(request){const incoming=new URL(request.url);let action=String(incoming.searchParams.get('action')||'').trim().toLowerCase();let data={};if(request.method==='POST'){try{data=await request.json()}catch(_){data={}}action=String(data.action||action||'').trim().toLowerCase()}if(!ALLOWED_ACTIONS.has(action))return Response.json({success:false,message:'Action auth tidak diizinkan.'},{status:400,headers:{'Cache-Control':'no-store'}});const target=new URL(GAS);target.searchParams.set('action',action);if(request.method==='POST'){Object.keys(data).forEach(key=>{if(!['action','callback','prefix','_'].includes(key))target.searchParams.set(key,String(data[key]==null?'':data[key]))})}else{incoming.searchParams.forEach((value,key)=>{if(!['callback','prefix','_','action'].includes(key))target.searchParams.set(key,value)})}const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),20000);try{const upstream=await fetch(target.toString(),{method:'GET',redirect:'follow',cache:'no-store',signal:controller.signal,headers:{Accept:'application/json, text/plain;q=0.9, */*;q=0.8'}});const text=await upstream.text();let body;try{body=JSON.parse(text)}catch(_){body={success:false,message:'Server auth mengembalikan respons yang tidak valid.'}}return Response.json(body,{status:upstream.ok?200:upstream.status,headers:{'Cache-Control':'no-store, no-cache, must-revalidate','Pragma':'no-cache','X-Kuda-Auth-Proxy':'v3'}})}catch(error){const aborted=error&&error.name==='AbortError';return Response.json({success:false,message:aborted?'Server auth timeout.':'Gagal menghubungi server auth.'},{status:504,headers:{'Cache-Control':'no-store'}})}finally{clearTimeout(timer)}}
export async function onRequestGet({request}){return handleAuth(request)}
export async function onRequestPost({request}){return handleAuth(request)}
