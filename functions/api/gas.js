const GAS='https://script.google.com/macros/s/AKfycbyUB8drjL1dSJedYjKIKjVc5gzIE3Pe-QS0FF8o1_zU4NkAweGLFquhHLfy1Nt_eITA-Q/exec';
const READ_ACTIONS=new Set(['data','announcement','userloginmode','getqueueorder','checkids','youtubemappings','youtubecheck']);
const PUBLIC_CACHE_ACTIONS=new Set(['data','announcement','userloginmode','getqueueorder','youtubecheck']);
const ALLOWED_ACTIONS=new Set(['data','announcement','health','add','addbatch','check','checkids','youtubecheck','updatestatus','markplayed','updatestatuses','delete','deletebatch','reorder','getqueueorder','setqueueorder','youtubemappings','saveyoutubemapping','deleteyoutubemapping','userloginmode','setuserloginmode','users','saveuser','deleteuser','adminresetpassword','saveannouncement','clearannouncement','login','session','logout','adminlogin','adminsession','adminlogout','forgotpassword','changepassword']);
const CANONICAL_ACTIONS={add:'add',addbatch:'addBatch',check:'check',checkids:'checkIds',youtubecheck:'youtubeCheck',updatestatus:'updateStatus',markplayed:'markPlayed',updatestatuses:'updateStatuses',delete:'delete',deletebatch:'deleteBatch',reorder:'setQueueOrder',getqueueorder:'getQueueOrder',setqueueorder:'setQueueOrder',userloginmode:'userloginmode',setuserloginmode:'setuserloginmode',youtubemappings:'youtubemappings',saveyoutubemapping:'saveyoutubemapping',deleteyoutubemapping:'deleteyoutubemapping',adminlogin:'adminlogin',adminsession:'adminsession',adminlogout:'adminlogout',adminresetpassword:'adminresetpassword',saveuser:'saveuser',deleteuser:'deleteuser',saveannouncement:'saveannouncement',clearannouncement:'clearannouncement',forgotpassword:'forgotpassword',changepassword:'changepassword'};
const EDGE_CACHE_SECONDS=30,STALE_WHILE_REVALIDATE_SECONDS=90,READ_TIMEOUT_MS=12000,WRITE_TIMEOUT_MS=15000;
function parseUpstream(text){const raw=String(text||'').trim();try{return JSON.parse(raw)}catch(_){}const m=raw.match(/^[^(]+\((.*)\)\s*;?\s*$/s);if(m)try{return JSON.parse(m[1])}catch(_){}return null}
function cacheKey(request){const u=new URL(request.url);['callback','prefix','_','_refresh'].forEach(k=>u.searchParams.delete(k));const entries=[...u.searchParams.entries()].sort((a,b)=>a[0].localeCompare(b[0])||a[1].localeCompare(b[1]));u.search='';entries.forEach(([k,v])=>u.searchParams.append(k,v));return new Request(u.toString(),{method:'GET'})}
function canCache(action,incoming){return PUBLIC_CACHE_ACTIONS.has(action)&&!incoming.searchParams.has('token')&&!incoming.searchParams.has('adminToken')}
function jsonp(cb,data,cacheable,cacheState){const safe=String(cb||'').replace(/[^a-zA-Z0-9_.$]/g,'')||'__kudajituCallback';const h={'Content-Type':'application/javascript; charset=utf-8','Cache-Control':cacheable?`public, max-age=0, s-maxage=${EDGE_CACHE_SECONDS}, stale-while-revalidate=${STALE_WHILE_REVALIDATE_SECONDS}`:'no-store, no-cache, must-revalidate'};if(cacheState)h['X-Kuda-Cache']=cacheState;return new Response(safe+'('+JSON.stringify(data)+');',{headers:h})}
function headers(cache,extra){return Object.assign({'Cache-Control':cache?`public, max-age=0, s-maxage=${EDGE_CACHE_SECONDS}, stale-while-revalidate=${STALE_WHILE_REVALIDATE_SECONDS}`:'no-store, no-cache, must-revalidate','X-Kuda-API':'v6'},extra||{})}
function normalizeAction(incoming){const action=String(incoming.searchParams.get('action')||'data').trim().toLowerCase();return action==='reorder'?'setqueueorder':action}
function normalizeTargetParams(incoming,action){const params=new URLSearchParams();incoming.searchParams.forEach((v,k)=>{if(!['callback','prefix','_','_refresh'].includes(k))params.append(k,v)});if(action==='setqueueorder'&&incoming.searchParams.has('ids')&&!incoming.searchParams.has('order')){const ids=String(incoming.searchParams.get('ids')||'').split(',').map(x=>x.trim()).filter(Boolean);params.delete('ids');params.set('order',JSON.stringify(ids));}params.set('action',CANONICAL_ACTIONS[action]||action);return params}
export async function onRequestGet({request}){const started=Date.now(),incoming=new URL(request.url),requestedAction=String(incoming.searchParams.get('action')||'data').trim().toLowerCase(),action=normalizeAction(incoming),cb=incoming.searchParams.get('callback')||incoming.searchParams.get('prefix')||'',isHealth=action==='health';if(!ALLOWED_ACTIONS.has(requestedAction))return cb?jsonp(cb,{success:false,message:'Action tidak diizinkan.'},false,'BYPASS'):Response.json({success:false,message:'Action tidak diizinkan.'},{status:400,headers:headers(false,{'X-Kuda-Cache':'BYPASS'})});const isRead=READ_ACTIONS.has(action),cacheable=canCache(action,incoming),cache=caches.default,key=cacheKey(request),forceFresh=incoming.searchParams.has('_refresh');if(cacheable&&!isHealth&&!forceFresh){const hit=await cache.match(key);if(hit){const t=await hit.text(),b=parseUpstream(t);if(b)return cb?jsonp(cb,b,true,'HIT'):new Response(t,{status:hit.status,headers:headers(true,{'X-Kuda-Cache':'HIT'})})}}const target=new URL(GAS),params=normalizeTargetParams(incoming,action);params.forEach((v,k)=>target.searchParams.append(k,v));const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),isHealth?8000:(isRead?READ_TIMEOUT_MS:WRITE_TIMEOUT_MS));try{const upstream=await fetch(target.toString(),{method:'GET',redirect:'follow',cache:'no-store',signal:controller.signal,headers:{Accept:'application/json, text/plain;q=0.9, */*;q=0.8'}});const upstreamText=await upstream.text(),body=parseUpstream(upstreamText),runtimeMs=Date.now()-started;if(!body){const e={success:false,message:'Server mengembalikan respons tidak valid.'};return cb?jsonp(cb,e,false,'BYPASS'):Response.json(e,{status:502,headers:headers(false,{'X-Kuda-Upstream-Ms':String(runtimeMs),'X-Kuda-Cache':'BYPASS'})})}if(isHealth){const health={success:upstream.ok&&body.success!==false,api:'online',upstream:upstream.ok?'reachable':'error',upstreamHttpStatus:upstream.status,healthActionOk:body.success!==false,latencyMs:runtimeMs,apiVersion:body.apiVersion||null};return cb?jsonp(cb,Object.assign({},body,health),false,'BYPASS'):Response.json(Object.assign({},body,health),{status:upstream.ok?200:502,headers:headers(false,{'X-Kuda-Upstream-Ms':String(runtimeMs),'X-Kuda-Cache':'BYPASS'})})}
// Writes intentionally do not purge every data/status key. Cached public reads expire quickly,
// while the Apps Script cache is invalidated by the mutation itself.
if(cacheable&&upstream.ok){const stored=Response.json(body,{status:200,headers:headers(true,{'X-Kuda-Cache':'MISS','X-Kuda-Upstream-Ms':String(runtimeMs)})});await cache.put(key,stored.clone())}const cacheState=cacheable&&upstream.ok?'MISS':'BYPASS',responseHeaders=headers(cacheable&&upstream.ok,{'X-Kuda-Upstream-Ms':String(runtimeMs),'X-Kuda-Cache':cacheState});return cb?jsonp(cb,body,cacheable&&upstream.ok,cacheState):Response.json(body,{status:upstream.ok?200:502,headers:responseHeaders})}catch(error){const runtimeMs=Date.now()-started,body={success:false,message:error&&error.name==='AbortError'?(isHealth?'Health check timeout.':'Server timeout.'):'Gagal menghubungi server.',api:'online',upstream:'error',latencyMs:runtimeMs};return cb?jsonp(cb,body,false,'BYPASS'):Response.json(body,{status:504,headers:headers(false,{'X-Kuda-Upstream-Ms':String(runtimeMs),'X-Kuda-Cache':'BYPASS'})})}finally{clearTimeout(timer)}}
