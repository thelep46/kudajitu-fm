(function(){'use strict';
const API='/api/gas';
const $=id=>document.getElementById(id);
let rows=[], timer=0, seq=0;
function enc(v){return encodeURIComponent(String(v==null?'':v))}
function ytId(v){const raw=String(v||'').trim();if(!raw)return'';try{const u=new URL(raw),h=u.hostname.toLowerCase().replace(/^www\./,'');if(h==='youtu.be')return(u.pathname.split('/').filter(Boolean)[0]||'').slice(0,11);if(['youtube.com','m.youtube.com','music.youtube.com'].includes(h)){const id=u.searchParams.get('v');if(id)return id.slice(0,11);const p=u.pathname.split('/').filter(Boolean);if(['shorts','embed','live'].includes(p[0])&&p[1])return p[1].slice(0,11)}}catch(e){}return''}
function jsonp(url,timeout=12000){return new Promise((resolve,reject)=>{const cb='__kuda_batch_v2_'+Date.now()+'_'+Math.random().toString(36).slice(2);const s=document.createElement('script');let done=false,t=setTimeout(()=>finish(new Error('Timeout')),timeout);function finish(e,d){if(done)return;done=true;clearTimeout(t);clearTimeout(t);s.remove();try{delete window[cb]}catch(_){}e?reject(e):resolve(d)}window[cb]=d=>d&&d.success===false?finish(new Error(d.message||'Server gagal')):finish(null,d);s.src=url+(url.includes('?')?'&':'?')+'callback='+cb+'&_='+Date.now();s.onerror=()=>finish(new Error('Network error'));document.head.appendChild(s)})}
function wrap(){const f=$('batchForm'),old=$('batch');if(!f||!old)return null;if($('youtubeBatchV2'))return $('youtubeBatchV2');const host=document.createElement('div');host.id='youtubeBatchV2';old.classList.add('hidden');const toolbar=document.createElement('div');toolbar.className='flex items-center justify-between gap-2 mb-3';toolbar.innerHTML='<span class="text-xs font-semibold text-gray-300">Daftar Lagu</span><button type="button" id="addBatchSong" class="btn px-3 py-2 rounded-lg bg-gray-950/80 border border-teal-800/50 text-[10px] font-bold text-teal-300">+ Tambah Lagu</button>';host.appendChild(toolbar);const list=document.createElement('div');list.id='batchSongList';list.className='space-y-3';host.appendChild(list);const note=document.createElement('p');note.id='batchV2State';note.className='text-[10px] text-gray-500 mt-2';host.appendChild(note);old.after(host);$('addBatchSong').onclick=()=>{if(rows.length>=3){note.textContent='Maksimal 3 lagu per batch.';return}rows.push({title:'',artist:'',url:'',status:'idle',videoId:'',token:++seq});render()};rows.push({title:'',artist:'',url:'',status:'idle',videoId:'',token:++seq});return host}
function render(){const list=$('batchSongList'),note=$('batchV2State');if(!list)return;list.innerHTML='';rows.forEach((r,i)=>{const card=document.createElement('div');card.className='rounded-xl border border-teal-900/60 bg-gray-950/40 p-3';card.innerHTML='<div class="flex items-center justify-between mb-2"><span class="text-[10px] font-bold text-gray-400">Lagu '+(i+1)+'</span>'+(rows.length>1?'<button type="button" data-remove="'+i+'" class="text-[10px] text-red-400">Hapus</button>':'')+'</div><div class="grid gap-2"><input class="field" data-title="'+i+'" maxlength="100" placeholder="Judul Lagu" value="'+esc(r.title)+'"><input class="field" data-artist="'+i+'" maxlength="80" placeholder="Penyanyi / Band" value="'+esc(r.artist)+'"><input class="field" data-url="'+i+'" maxlength="200" type="url" placeholder="Link YouTube (jika belum terdaftar)" value="'+esc(r.url)+'"></div><div data-state="'+i+'" class="text-[10px] text-gray-500 mt-2">'+state(r)+'</div>';list.appendChild(card)});list.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>{rows.splice(Number(b.dataset.remove),1);render();schedule()});list.querySelectorAll('[data-title]').forEach(e=>e.oninput=()=>{rows[Number(e.dataset.title)].title=e.value;schedule()});list.querySelectorAll('[data-artist]').forEach(e=>e.oninput=()=>{rows[Number(e.dataset.artist)].artist=e.value;schedule()});list.querySelectorAll('[data-url]').forEach(e=>e.oninput=()=>{const i=Number(e.dataset.url);rows[i].url=e.value;rows[i].status=ytId(e.value)?'user':'idle';schedule()});if(note)note.textContent='Maksimal 3 lagu. Mapping otomatis dicek per lagu.'}
function esc(v){return String(v||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
function state(r){if(r.status==='loading')return'🔎 Memeriksa mapping YouTube...';if(r.status==='mapping')return'✅ Video sudah terdaftar — link otomatis digunakan.';if(r.status==='user')return'✅ Link YouTube valid.';if(r.status==='missing')return'⚠️ Video belum terdaftar — masukkan link YouTube.';if(r.status==='error')return'❌ Mapping gagal diverifikasi.';if(!r.title||!r.artist)return'Isi Judul dan Penyanyi.';return'🔎 Akan memeriksa mapping otomatis.'}
async function checkOne(i){const r=rows[i];if(!r.title||!r.artist)return;r.status='loading';const my=++seq;r.token=my;const direct=ytId(r.url);if(direct){r.videoId=direct;r.status='user';render();return}render();try{const x=await jsonp(API+'?action=youtubeCheck&title='+enc(r.title)+'&artist='+enc(r.artist));if(r.token!==my)return;if(x&&x.found&&x.videoId){r.videoId=String(x.videoId);r.url='https://www.youtube.com/watch?v='+r.videoId;r.status='mapping'}else{r.videoId='';r.status='missing'} }catch(e){if(r.token!==my)return;r.videoId='';r.status='error'}render()}
function schedule(){clearTimeout(timer);timer=setTimeout(()=>rows.forEach((_,i)=>checkOne(i)),450)}
async function submit(e){e.preventDefault();e.stopImmediatePropagation();const name=($('name')?.value||'').trim();if(!name)return alert('Nama wajib diisi.');const valid=rows.filter(r=>r.title&&r.artist);if(!valid.length)return alert('Isi minimal satu lagu.');await Promise.all(valid.map((_,i)=>checkOne(i)));const invalid=rows.filter(r=>r.title||r.artist).filter(r=>!r.title||!r.artist||!r.videoId);if(invalid.length)return alert('Setiap lagu harus memiliki mapping YouTube atau link YouTube.');const payload=valid.map(r=>({id:'REQ-'+Date.now().toString(36)+'-'+Math.random().toString(36).slice(2,7),requester:name,title:r.title,artist:r.artist,note:r.url||('https://www.youtube.com/watch?v='+r.videoId),timestamp:new Date().toISOString(),status:'pending',votes:1}));const btn=$('batchBtn');try{if(typeof window.busy==='function')window.busy(btn,true,'Mengirim...');const u=new URL(API,location.origin);u.searchParams.set('action','addBatch');u.searchParams.set('requester',name);u.searchParams.set('items',JSON.stringify(payload));await jsonp(u.toString(),20000);if(typeof window.toast==='function')window.toast('Semua request berhasil dikirim.');rows=[];rows.push({title:'',artist:'',url:'',status:'idle',videoId:'',token:++seq});render();if(typeof window.loadData==='function')await window.loadData(true)}catch(err){if(typeof window.toast==='function')window.toast('Batch request gagal: '+(err.message||err),'error');else alert('Batch request gagal: '+err.message)}finally{if(typeof window.busy==='function')window.busy(btn,false)}}
function patch(){const f=$('batchForm');if(!f||f.dataset.youtubeBatchV2)return;f.dataset.youtubeBatchV2='1';wrap();render();f.addEventListener('submit',submit,true)}
function init(){patch()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
